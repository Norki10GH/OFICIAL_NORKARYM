// client/src/js/pages/inscripcio.js

import { initParticleAnimation } from '../components/particle-animation.js';

export function inscripcioPage() {
  // Inicialitza l'animació de partícules específica per a aquesta pàgina.
  initParticleAnimation();

  // Aquí anirà la lògica futura per als formularis de registre i inici de sessió.
}