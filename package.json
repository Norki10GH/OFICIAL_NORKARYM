{
  "name": "norkarym-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm:dev:vite\" \"npm:dev:firebase\"",
    "dev:vite": "vite",
    "dev:firebase": "firebase emulators:start --only functions",
    "build": "vite build"
  },
  "dependencies": {
    "express": "^5.1.0",
    "firebase": "^12.4.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "firebase-tools": "^13.13.2",
    "vite": "^7.1.10"
  }
}
