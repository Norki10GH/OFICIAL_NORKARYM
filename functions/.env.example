# ===================================================================
#           ARXIU D'EXEMPLE DE VARIABLES D'ENTORN (.env)
# ===================================================================
#
# Aquest arxiu serveix com a plantilla. Has de crear el teu propi
# arxiu anomenat '.env' a la mateixa carpeta ('/functions') i
# omplir-lo amb les teves credencials reals.
#
# MAI PUGIS L'ARXIU .env A UN REPOSITORI DE GIT.
# Està inclòs a .gitignore per seguretat.
#
# -------------------------------------------------------------------

# --- DADES DE LA BASE DE DADES DE POSTGRESQL A GOOGLE CLOUD SQL ---

# Nom d'usuari de la base de dades.
DB_USER="el_teu_usuari_de_postgres"

# Contrasenya de l'usuari de la base de dades.
DB_PASSWORD="la_teva_contrasenya_segura"

# Nom de la base de dades a la qual et vols connectar.
DB_DATABASE="gestio_usuaris"

# --- DADES PER A LA CONNEXIÓ LOCAL AMB EL CLOUD SQL AUTH PROXY ---

# Host on s'executa el proxy. Normalment, és la teva màquina local.
DB_HOST="127.0.0.1"

# Port que has configurat al Cloud SQL Auth Proxy. Per defecte, PostgreSQL
# utilitza el 5432, però pots canviar-lo si cal.
DB_PORT="5432"

# --- DADES DE LA INSTÀNCIA DE GOOGLE CLOUD SQL ---

# Aquest és l'identificador únic de la teva instància a Google Cloud.
# El pots trobar a la pàgina de detalls de la instància a la consola de Google Cloud.
# Format: "el-teu-projecte-id:la-teva-regio:el-nom-de-la-teva-instancia"
INSTANCE_CONNECTION_NAME="norkarym:europe-west1:norkarym-db"
